module.exports = {
  // 默认时区，可以设置为 'Asia/Tokyo', 'Asia/Shanghai' 等
  // 完整的时区列表可以在这里找到：https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
  timezone: process.env.TIMEZONE || 'Asia/Tokyo'
}; 